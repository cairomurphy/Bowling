@{
}

@model List<Bowler>

<h3>Available Teams</h3>

@if (Model?.Count() > 0)
{
    <h1>Welcome @ViewBag.Name.TeamName</h1>
    @foreach (Bowler b in Model)
    {
        <p>@b.BowlerFirstName and @b.Team.TeamName</p>
    }
    @foreach (Team t in ViewBag.Teams)
    {

    }

    @*@foreach (Bowler b in Model)
    {

        <h1>Welcome @b.Team.TeamName</h1>

        @foreach (Team t in ViewBag.Teams)
        {
            @if (t.TeamID == b.TeamID)
            {
                <form method="post">

                    <button class="btn btn-danger" asp-route-teamid="@t.TeamID" type="submit">@t.TeamName</button>
                </form>
                <p>@b.BowlerFirstName</p>
            }
            else
            {
                <form method="post">

                    <button class="btn btn-primary" asp-route-teamid="@t.TeamID" type="submit">@t.TeamName</button>
                </form>
            }
        }
    }*@
}
else
{
    <h1>Pick a team to see the players</h1>
    @foreach (Team t in ViewBag.Teams)
    {
        <form method="post">

            <button class="btn btn-primary" asp-route-teamname="@t.TeamName" asp-route-teamid="@t.TeamID" type="submit">@t.TeamName</button>
        </form>
    }
}
//display teams
//once clicked populate with names
//on get populate the heading with choose
//on post